Release notes for integration-r3

New features:
- The first version of the IAEA benchmark geometry
- Added fragment energy distribution data
- ROOT script preparing an IAEA benchmark figure with data

Bugfixes:
- HadrontherapySteppingAction: Fixed the name of the phantom solid name.
- Fixed the initialization of the histograms and analysis objects
  (HadrontherapyAnalysisManager::book() was not called properly)

Unresolved issues:
- "Warning! Unknown proton process: ionIoni" error message produced by
  the HadrontherapySteppingManager